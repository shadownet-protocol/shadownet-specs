{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:shadownet:0.2:schema:envelope",
  "title": "Shadownet Envelope JWT payload",
  "description": "Payload of a shadownet-env+jwt. Defined in rfcs/0001-shadownet.md §8.3. Carried as a JWS-compact string in the A2A message's metadata under key 'urn:shadownet:0.2'.",
  "type": "object",
  "required": ["v", "from", "to", "iat", "exp", "msgHash", "body"],
  "additionalProperties": true,
  "properties": {
    "v": {
      "type": "string",
      "const": "0.2",
      "description": "Protocol version."
    },
    "from": {
      "type": "string",
      "description": "Sender Shadowname (canonical lowercase). MUST equal the JWS header `kid`.",
      "pattern": "^[a-z0-9._-]{1,63}@[a-z0-9.-]+$"
    },
    "to": {
      "type": "string",
      "description": "Recipient Shadowname (canonical lowercase).",
      "pattern": "^[a-z0-9._-]{1,63}@[a-z0-9.-]+$"
    },
    "iat": {
      "type": "integer",
      "description": "Issued-at, Unix seconds.",
      "minimum": 0
    },
    "exp": {
      "type": "integer",
      "description": "Expiry, Unix seconds. Conformance suite enforces exp - iat ≤ 300 (5 minutes) per §8.3.",
      "minimum": 0
    },
    "msgHash": {
      "type": "string",
      "description": "sha256:<base64url> hash of canonical JSON of the A2A message with the Shadownet metadata key removed. Computed per §8.4.",
      "pattern": "^sha256:[A-Za-z0-9_-]+$"
    },
    "body": {
      "type": "object",
      "description": "Application content. See §8.5.",
      "additionalProperties": true,
      "properties": {
        "text": {
          "type": "string",
          "description": "Human-readable message text. RECOMMENDED on every envelope."
        },
        "intent": {
          "type": "string",
          "format": "uri",
          "description": "Application-defined identifier for the kind of interaction. Receivers MUST surface envelopes with unknown intents."
        },
        "data": {
          "type": "object",
          "description": "Application-defined structured payload, schema named by `intent` when present."
        }
      }
    },
    "creds": {
      "type": "array",
      "description": "Credentials presented with this envelope. Included on first contact, or after receiver's cached credentials for this sender expire.",
      "items": {
        "type": "string",
        "description": "JWS-compact credential string (shadownet-cred+jwt)."
      }
    }
  }
}